name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello Satish! Welcome to GitHub Actions."
      - name: Show Date
        run: date
      - name: Show Current Folder
        run: pwd